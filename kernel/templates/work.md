# Work: <mission name>

<!-- The concrete plan/tasks that perform the Mission's Roles. The Mission's `## Roles` is the accountable
     execution structure (who owns what); this worklist is how that work actually gets done, week to week.
     One layer, not two: tasks here perform the Roles in mission.md; don't add a decomposition between. -->

## Handoffs
<!-- Where work moves between people. Read first each session; clear items when actioned. -->
- [ ] to: <who> · re: <what> · status: <approved | blocked> · next: <the exact next action for them>

## Next
<!-- This is the Mission's committed executable backlog, not an idea list or a list of future Missions.
     Keep each work reference unique in this Mission. Append `delivery: <branch or review link>` only when one exists. -->
- [ ] <MISSION-ID>-W<NN> · <work item> · Role: <Mission Role> · owner: <who> · output: <what it produces> · done when: <observable result, not only that an artefact exists> · needs: <dependency or generic capability, if any; e.g. CRM read access, not a connector chosen before the need>

## In progress
<!-- Work being performed now. -->
- [ ] <MISSION-ID>-W<NN> · <item>

## Blocked
<!-- Waiting for an exact decision, dependency or access condition. -->
- [ ] <MISSION-ID>-W<NN> · <item> · DECISION NEEDED: <the exact call> · options: A / B / C · for: <who decides>

## Done
<!-- Completed work, with what was produced and where. -->
- [x] <MISSION-ID>-W<NN> · <item> · produced: <what, and where in outputs/>

## Findings
<!-- Claims learned along the way keep status, source and freshness separate:
     [confirmed <date> · source: <named person or source>]
     [reported <date> · source: <named person or source>]
     [inferred <date> · from: <evidence>]
     [hypothesis <date> · test: <what would settle it>]
     [simulated <date>] for examples that did not happen.
     Surface the label on read-back. Re-check old or reported claims before relying on them.
     Legacy [unverified] means reported but not checked; translate it when the record is next touched. -->
- <claim learned>, [reported <date> · source: <source>]
